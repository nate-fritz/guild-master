@page "/"
@using GuildMaster.Services
@inject GameConsole GameConsole
@inject GameEngine GameEngine
@inject IJSRuntime JS

<PageTitle>GuildMaster (Alpha Build)</PageTitle>

<div class="game-container">
    <div class="console-output" id="consoleOutput" @ref="outputDiv">
        @for (int i = 0; i < outputLines.Count; i++)
        {
            @if (unreadMarkerIndex == i)
            {
                <div class="unread-divider">new</div>
            }
            <div>@((MarkupString)outputLines[i])</div>
        }
    </div>

    <div class="jump-pill" @onclick="JumpToPresent" title="Ctrl+End">&#9660; new text below</div>

    <div class="console-input">
        <span class="prompt">&gt;</span>
        <input @ref="inputField"
               @bind="currentInput"
               @bind:event="oninput"
               @onkeydown="HandleKeyDown"
               placeholder="What do you do?" />
    </div>
</div>

@code {
    private List<string> outputLines = new();
    private string currentInput = "";
    private ElementReference outputDiv;
    private ElementReference inputField;
    private bool awaitingName = false;
    private bool awaitingClass = false;
    private string pendingPlayerName = "";
    private List<string> commandHistory = new();
    private int historyIndex = -1;
    private const int MAX_HISTORY_SIZE = 25;

    // Scroll UX state: pinned = view glued to the bottom; the unread marker
    // sits before the first line that arrived while the player was scrolled up.
    private bool scrollPinned = true;
    private int? unreadMarkerIndex = null;
    private DotNetObjectReference<Home>? selfRef;

    [JSInvokable]
    public void OnScrollPinnedChanged(bool pinned)
    {
        scrollPinned = pinned;
    }

    [JSInvokable]
    public void OnViewportResized(int lines)
    {
        GuildMaster.Helpers.TextHelper.ViewportLinesPerPage = lines;
    }

    private async Task JumpToPresent()
    {
        await JS.InvokeVoidAsync("gmConsole.scrollToBottom");
        await inputField.FocusAsync();
    }

    protected override void OnInitialized()
    {
        GameConsole.OnOutput += HandleOutput;
        GameConsole.OnClear += HandleClear;

        GuildMaster.Services.Console.Initialize(GameConsole);
        GuildMaster.Services.AnsiConsole.Initialize(GameConsole);

        // Set callback for combat state changes
        GameEngine.SetStateChangedCallback(() => InvokeAsync(StateHasChanged));

        ShowTitle();
    }
    
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            selfRef = DotNetObjectReference.Create(this);
            await JS.InvokeVoidAsync("gmConsole.init", "consoleOutput", selfRef);
            GuildMaster.Helpers.TextHelper.ViewportLinesPerPage =
                await JS.InvokeAsync<int>("gmConsole.measureViewportLines");
            await inputField.FocusAsync();
        }

        // Follows output only while the player is at the bottom; scrolled-up
        // readers stay where they are (the jump pill offers the way back).
        try
        {
            await JS.InvokeVoidAsync("gmConsole.afterRender");
        }
        catch
        {
            // Ignore scroll errors during teardown/initialization
        }
    }
    
    private void ShowTitle()
    {
        GameConsole.MarkupLine("");
        GameConsole.MarkupLine("[#FA935F]██╗    ██╗███████╗██╗      ██████╗ ██████╗ ███╗   ███╗███████╗[/]");
        GameConsole.MarkupLine("[#FA8448]██║    ██║██╔════╝██║     ██╔════╝██╔═══██╗████╗ ████║██╔════╝[/]");
        GameConsole.MarkupLine("[#FC7938]██║ █╗ ██║█████╗  ██║     ██║     ██║   ██║██╔████╔██║█████╗  [/]");
        GameConsole.MarkupLine("[#FA6419]██║███╗██║██╔══╝  ██║     ██║     ██║   ██║██║╚██╔╝██║██╔══╝  [/]");
        GameConsole.MarkupLine("[#FA5A0A]╚███╔███╔╝███████╗███████╗╚██████╗╚██████╔╝██║ ╚═╝ ██║███████╗[/]");
        GameConsole.MarkupLine("[#BA3E00] ╚══╝╚══╝ ╚══════╝╚══════╝ ╚═════╝ ╚═════╝ ╚═╝     ╚═╝╚══════╝[/]");
        GameConsole.MarkupLine("");
        GameConsole.MarkupLine("[#FA935F]                    ████████╗ ██████╗ [/]");
        GameConsole.MarkupLine("[#FA8448]                    ╚══██╔══╝██╔═══██╗[/]");
        GameConsole.MarkupLine("[#FC7938]                       ██║   ██║   ██║[/]");
        GameConsole.MarkupLine("[#FA6419]                       ██║   ██║   ██║[/]");
        GameConsole.MarkupLine("[#FA5A0A]                       ██║   ╚██████╔╝[/]");
        GameConsole.MarkupLine("[#BA3E00]                       ╚═╝    ╚═════╝ [/]");
        GameConsole.MarkupLine("");
        GameConsole.MarkupLine("[#FA935F] ██████╗ ██╗   ██╗██╗██╗     ██████╗ ███╗   ███╗ █████╗ ███████╗████████╗███████╗██████╗ [/]");
        GameConsole.MarkupLine("[#FA8448]██╔════╝ ██║   ██║██║██║     ██╔══██╗████╗ ████║██╔══██╗██╔════╝╚══██╔══╝██╔════╝██╔══██╗[/]");
        GameConsole.MarkupLine("[#FC7938]██║  ███╗██║   ██║██║██║     ██║  ██║██╔████╔██║███████║███████╗   ██║   █████╗  ██████╔╝[/]");
        GameConsole.MarkupLine("[#FA6419]██║   ██║██║   ██║██║██║     ██║  ██║██║╚██╔╝██║██╔══██║╚════██║   ██║   ██╔══╝  ██╔══██╗[/]");
        GameConsole.MarkupLine("[#FA5A0A]╚██████╔╝╚██████╔╝██║███████╗██████╔╝██║ ╚═╝ ██║██║  ██║███████║   ██║   ███████╗██║  ██║[/]");
        GameConsole.MarkupLine("[#BA3E00] ╚═════╝  ╚═════╝ ╚═╝╚══════╝╚═════╝ ╚═╝     ╚═╝╚═╝  ╚═╝╚══════╝   ╚═╝   ╚══════╝╚═╝  ╚═╝[/]");
        GameConsole.MarkupLine("");
        GameConsole.MarkupLine("[#FA8448]                            A Text Adventure Game[/]");
        GameConsole.MarkupLine("");
        GameConsole.MarkupLine("");
        GameConsole.MarkupLine("");
        GameConsole.MarkupLine("[#90FF90]1.[/] New Game");
        GameConsole.MarkupLine("[#75C8FF]2.[/] Load Game");
        GameConsole.MarkupLine("");
        GameConsole.MarkupLine("[dim](Enter a number to choose)[/]");
    }

    private async void HandleOutput(string htmlLine)
    {
        // Text arriving while the player reads scrollback: mark where the
        // unseen part begins (once), so the divider shows them the boundary.
        if (!scrollPinned && unreadMarkerIndex == null)
        {
            unreadMarkerIndex = outputLines.Count;
        }
        outputLines.Add(htmlLine);
        await InvokeAsync(StateHasChanged);
    }

    private async void HandleClear()
    {
        outputLines.Clear();
        unreadMarkerIndex = null;
        await InvokeAsync(StateHasChanged);
    }

    private async Task HandleKeyDown(KeyboardEventArgs e)
    {
        // Scrollback navigation from the input field
        if (e.Key == "PageUp")
        {
            await JS.InvokeVoidAsync("gmConsole.scrollPage", -1);
            return;
        }
        if (e.Key == "PageDown")
        {
            await JS.InvokeVoidAsync("gmConsole.scrollPage", 1);
            return;
        }
        if (e.Key == "End" && e.CtrlKey)
        {
            await JumpToPresent();
            return;
        }

        if (e.Key == "Enter")
        {
            string input = currentInput?.Trim() ?? "";
            currentInput = "";

            // Submitting a command while at the bottom means the player has
            // caught up; retire the unread divider.
            if (scrollPinned && unreadMarkerIndex != null)
            {
                unreadMarkerIndex = null;
            }

            // Add non-empty commands to history
            if (!string.IsNullOrEmpty(input))
            {
                commandHistory.Add(input);
                // Limit history size
                if (commandHistory.Count > MAX_HISTORY_SIZE)
                {
                    commandHistory.RemoveAt(0);
                }
                // Reset history index
                historyIndex = -1;
            }

            // Always process if there's input, or if it's empty but the game wants it (for pagination)
            if (!string.IsNullOrEmpty(input) || GameEngine.IsGameStarted)
            {
                if (!string.IsNullOrEmpty(input))
                {
                    outputLines.Add($"<span style=\"color:#44ff44\">&gt; {System.Web.HttpUtility.HtmlEncode(input)}</span>");
                }
                await ProcessCommand(input);
                await InvokeAsync(StateHasChanged);
            }
        }
        else if (e.Key == "ArrowUp")
        {
            // Navigate to previous command (older)
            if (commandHistory.Count > 0)
            {
                if (historyIndex == -1)
                {
                    // Start from the most recent command
                    historyIndex = commandHistory.Count - 1;
                }
                else if (historyIndex > 0)
                {
                    // Move to older command
                    historyIndex--;
                }

                if (historyIndex >= 0 && historyIndex < commandHistory.Count)
                {
                    currentInput = commandHistory[historyIndex];
                    await InvokeAsync(StateHasChanged);
                }
            }
        }
        else if (e.Key == "ArrowDown")
        {
            // Navigate to next command (newer) or clear input
            if (historyIndex != -1)
            {
                historyIndex++;
                if (historyIndex >= commandHistory.Count)
                {
                    // Clear input when reaching the end
                    currentInput = "";
                    historyIndex = -1;
                }
                else
                {
                    currentInput = commandHistory[historyIndex];
                }
                await InvokeAsync(StateHasChanged);
            }
        }
    }

    private async Task ProcessCommand(string command)
    {
        // If GameEngine is waiting for load slot selection, pass through directly
        if (GameEngine.IsWaitingForLoadSlot)
        {
            await GameEngine.ProcessCommand(command);
            // Only show status bar if not in an interactive state (dialogue/menu/shop/combat show their own)
            if (!GameEngine.IsInInteractiveState)
                GameEngine.DisplayStats();
            return;
        }

        if (awaitingName)
        {
            pendingPlayerName = string.IsNullOrEmpty(command) ? "Adventurer" : command;
            awaitingName = false;
            awaitingClass = true;

            GameConsole.MarkupLine("");
            GameConsole.MarkupLine("[#75C8FF]Choose your class:[/]");
            GameConsole.MarkupLine("");
            GameConsole.MarkupLine("[#FA935F]1. Legionnaire[/] - Heavily armored warrior that protects allies with sword and shield.");
            GameConsole.MarkupLine("   HP: 25 | EP: 8 | ATK: 2 | DEF: 3 | SPD: 1");
            GameConsole.MarkupLine("");
            GameConsole.MarkupLine("[#90FF90]2. Venator[/] - Swift ranger that uses a bow to deal high damage to enemies from afar.");
            GameConsole.MarkupLine("   HP: 18 | EP: 12 | ATK: 3 | DEF: 1 | SPD: 3");
            GameConsole.MarkupLine("");
            GameConsole.MarkupLine("[#75C8FF]3. Oracle[/] - Mystic that supports allies and devastates enemies with both divine and elemental magic.");
            GameConsole.MarkupLine("   HP: 15 | EP: 18 | ATK: 1 | DEF: 0 | SPD: 2");
            GameConsole.MarkupLine("");
            return;
        }

        if (awaitingClass)
        {
            // Validate class selection
            if (command != "1" && command != "2" && command != "3")
            {
                GameConsole.MarkupLine("");
                GameConsole.MarkupLine("[#FF9999]Invalid choice! Please select 1, 2, or 3.[/]");
                GameConsole.MarkupLine("");
                GameConsole.MarkupLine("[#75C8FF]Choose your class:[/]");
                GameConsole.MarkupLine("");
                GameConsole.MarkupLine("[#FA935F]1. Legionnaire[/] - Heavily armored warrior that protects allies with sword and shield.");
                GameConsole.MarkupLine("   HP: 25 | EP: 8 | ATK: 2 | DEF: 3 | SPD: 1");
                GameConsole.MarkupLine("");
                GameConsole.MarkupLine("[#90FF90]2. Venator[/] - Swift ranger that uses a bow to deal high damage to enemies from afar.");
                GameConsole.MarkupLine("   HP: 18 | EP: 12 | ATK: 3 | DEF: 1 | SPD: 3");
                GameConsole.MarkupLine("");
                GameConsole.MarkupLine("[#75C8FF]3. Oracle[/] - Mystic that supports allies and devastates enemies with both divine and elemental magic.");
                GameConsole.MarkupLine("   HP: 15 | EP: 18 | ATK: 1 | DEF: 0 | SPD: 2");
                GameConsole.MarkupLine("");
                return;
            }

            string className = command switch
            {
                "1" => "Legionnaire",
                "2" => "Venator",
                "3" => "Oracle",
                _ => "Legionnaire" // Should never hit this now
            };

            awaitingClass = false;
            GameConsole.MarkupLine($"\n[#90FF90]You have chosen: {className}[/]");
            GameConsole.MarkupLine("");

            GameEngine.StartNewGame(pendingPlayerName, className);
            // Only show status bar if not in an interactive state
            if (!GameEngine.IsInInteractiveState)
                GameEngine.DisplayStats();
            return;
        }

        // Handle start menu
        if (!GameEngine.IsGameStarted)
        {
            if (command == "1" || command.ToLower() == "new game" || command.ToLower() == "start")
            {
                GameConsole.MarkupLine("");
                GameConsole.MarkupLine("[#75C8FF]What should I call you?[/]");
                awaitingName = true;
                return;
            }
            else if (command == "2" || command.ToLower() == "load game" || command.ToLower() == "load")
            {
                GameConsole.MarkupLine("");
                await GameEngine.ProcessCommand("load");
                // Only show status bar if not in an interactive state
                if (!GameEngine.IsInInteractiveState)
                    GameEngine.DisplayStats();
                return;
            }
            else
            {
                GameConsole.MarkupLine("[dim]Please enter 1 for New Game or 2 for Load Game.[/]");
                return;
            }
        }

        if (GameEngine.IsGameStarted)
        {
            await GameEngine.ProcessCommand(command);
            // Only show status bar if not in an interactive state (dialogue/menu/shop/combat show their own)
            if (!GameEngine.IsInInteractiveState)
                GameEngine.DisplayStats();
        }
    }

    public void Dispose()
    {
        GameConsole.OnOutput -= HandleOutput;
        GameConsole.OnClear -= HandleClear;
        selfRef?.Dispose();
    }
}
